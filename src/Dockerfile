# Используем легкую версию Node.js
FROM node:18-alpine

# Создаем папку внутри контейнера
WORKDIR /usr/src/app

# Сначала копируем только файлы зависимостей (чтобы кэшировать установку)
COPY package*.json ./

# Устанавливаем библиотеки
RUN npm install

# Копируем весь остальной код
COPY . .

# Команда для запуска
CMD ["node", "index.js"]